<div class="main">
	<div class="container">
		<div class="row">
  			<% @sites.each do |s| %>
    			<div class="col-md-3">
       				<a href="#" class="thumbnail">
       					<img src="<%= s.image %>"/>
       				</a>
    			</div>
  			<% end %>
		</div>
	</div>
</div>